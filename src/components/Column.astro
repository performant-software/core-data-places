---
import clsx from "clsx";
import _ from "underscore";
import ColumnContent from "@components/ColumnContent.astro";

const { items, className, url, t, isTinaPreview } = Astro.props;
---

{ url && ( 
  <a
    href={url} 
    class={clsx(
      className,
      'flex flex-col'
    )}
  >
    { _.map(items, (item) => ( 
      <ColumnContent
        item={item}
        t={t}
        isTinaPreview={isTinaPreview}
      />
    ))}
  </a>
) }

{ !url && (
  <div 
    class={clsx(
      className,
      'flex flex-col'
    )}
  >
    { _.map(items, (item) => ( 
      <ColumnContent item={item} isTinaPreview={isTinaPreview} t={t} />
    ))}
  </div>
) }